find_package(Qt6 COMPONENTS Core Quick Qml REQUIRED)

qt_add_library(controller STATIC)

qt6_add_qml_module(controller
    URI "Controller"
    VERSION 1.0
    SOURCES
    {% for file in files %}
    {{ file }}
    {%- endfor %}

)

target_link_libraries(controller PRIVATE {{ application_name }}-controller)

target_link_libraries(controller
    PRIVATE Qt6::Quick Qt6::Core Qt6::Qml
)
