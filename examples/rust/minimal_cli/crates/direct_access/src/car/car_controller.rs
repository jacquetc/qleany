// Generated by Qleany. Edit at your own risk! Be careful when regenerating this file
// as changes will be lost.

use super::units_of_work::CarUnitOfWorkFactory;
use super::{
    dtos::{CarDto, CreateCarDto},
    units_of_work::CarUnitOfWorkROFactory,
    use_cases::{
        create_car_multi_uc::CreateCarMultiUseCase, create_car_uc::CreateCarUseCase,
        get_car_multi_uc::GetCarMultiUseCase, get_car_uc::GetCarUseCase,
        remove_car_multi_uc::RemoveCarMultiUseCase, remove_car_uc::RemoveCarUseCase,
        update_car_multi_uc::UpdateCarMultiUseCase, update_car_uc::UpdateCarUseCase,
    },
};
use anyhow::{Ok, Result};

use common::undo_redo::UndoRedoManager;
use common::{database::db_context::DbContext, event::EventHub, types::EntityId};
use std::sync::Arc;

pub fn create(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    entity: &CreateCarDto,
) -> Result<CarDto> {
    let uow_factory = CarUnitOfWorkFactory::new(&db_context, &event_hub);
    let mut uc = CreateCarUseCase::new(Box::new(uow_factory));
    let result = uc.execute(entity.clone())?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(result)
}

pub fn get(db_context: &DbContext, id: &EntityId) -> Result<Option<CarDto>> {
    let uow_factory = CarUnitOfWorkROFactory::new(&db_context);
    let uc = GetCarUseCase::new(Box::new(uow_factory));
    uc.execute(id)
}

pub fn update(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    entity: &CarDto,
) -> Result<CarDto> {
    let uow_factory = CarUnitOfWorkFactory::new(&db_context, &event_hub);
    let mut uc = UpdateCarUseCase::new(Box::new(uow_factory));
    let result = uc.execute(entity)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(result)
}

pub fn remove(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    id: &EntityId,
) -> Result<()> {
    let uow_factory = CarUnitOfWorkFactory::new(&db_context, &event_hub);
    let mut uc = RemoveCarUseCase::new(Box::new(uow_factory));
    uc.execute(id)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(())
}

pub fn create_multi(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    entities: &[CreateCarDto],
) -> Result<Vec<CarDto>> {
    let uow_factory = CarUnitOfWorkFactory::new(&db_context, &event_hub);
    let mut uc = CreateCarMultiUseCase::new(Box::new(uow_factory));
    let result = uc.execute(entities)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(result)
}

pub fn get_multi(db_context: &DbContext, ids: &[EntityId]) -> Result<Vec<Option<CarDto>>> {
    let uow_factory = CarUnitOfWorkROFactory::new(&db_context);
    let uc = GetCarMultiUseCase::new(Box::new(uow_factory));
    uc.execute(ids)
}

pub fn update_multi(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    entities: &[CarDto],
) -> Result<Vec<CarDto>> {
    let uow_factory = CarUnitOfWorkFactory::new(&db_context, &event_hub);
    let mut uc = UpdateCarMultiUseCase::new(Box::new(uow_factory));
    let result = uc.execute(entities)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(result)
}

pub fn remove_multi(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let uow_factory = CarUnitOfWorkFactory::new(&db_context, &event_hub);
    let mut uc = RemoveCarMultiUseCase::new(Box::new(uow_factory));
    uc.execute(ids)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(())
}
