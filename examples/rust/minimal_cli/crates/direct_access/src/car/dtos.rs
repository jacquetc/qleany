// Generated by Qleany. Edit at your own risk! Be careful when regenerating this file
// as changes will be lost.

use common::entities::Car;
use common::entities::CarStatus;
use common::types::EntityId;
use serde::{Deserialize, Serialize};
use std::convert::From;

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct CarDto {
    pub id: EntityId,
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub make: String,
    pub model: String,
    pub year: i64,
    pub price: f64,
    pub status: CarStatus,
}

impl From<CarDto> for Car {
    fn from(dto: CarDto) -> Self {
        Car {
            id: dto.id,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            make: dto.make,
            model: dto.model,
            year: dto.year,
            price: dto.price,
            status: dto.status,
        }
    }
}

impl From<&CarDto> for Car {
    fn from(dto: &CarDto) -> Self {
        Car {
            id: dto.id,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            make: dto.make.clone(),
            model: dto.model.clone(),
            year: dto.year.clone(),
            price: dto.price.clone(),
            status: dto.status.clone(),
        }
    }
}

impl From<Car> for CarDto {
    fn from(entity: Car) -> Self {
        CarDto {
            id: entity.id,
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            make: entity.make,
            model: entity.model,
            year: entity.year,
            price: entity.price,
            status: entity.status,
        }
    }
}

#[derive(Debug, Clone, PartialEq, Default, Serialize, Deserialize)]
pub struct CreateCarDto {
    pub created_at: chrono::DateTime<chrono::Utc>,
    pub updated_at: chrono::DateTime<chrono::Utc>,
    pub make: String,
    pub model: String,
    pub year: i64,
    pub price: f64,
    pub status: CarStatus,
}

impl From<CreateCarDto> for Car {
    fn from(dto: CreateCarDto) -> Self {
        Car {
            id: 0,
            created_at: dto.created_at,
            updated_at: dto.updated_at,
            make: dto.make,
            model: dto.model,
            year: dto.year,
            price: dto.price,
            status: dto.status,
        }
    }
}

impl From<&CreateCarDto> for Car {
    fn from(dto: &CreateCarDto) -> Self {
        Car {
            id: 0,
            created_at: dto.created_at.clone(),
            updated_at: dto.updated_at.clone(),
            make: dto.make.clone(),
            model: dto.model.clone(),
            year: dto.year.clone(),
            price: dto.price.clone(),
            status: dto.status.clone(),
        }
    }
}

impl From<Car> for CreateCarDto {
    fn from(entity: Car) -> Self {
        CreateCarDto {
            created_at: entity.created_at,
            updated_at: entity.updated_at,
            make: entity.make,
            model: entity.model,
            year: entity.year,
            price: entity.price,
            status: entity.status,
        }
    }
}
