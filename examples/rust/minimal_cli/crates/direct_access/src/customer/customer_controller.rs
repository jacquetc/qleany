// Generated by Qleany. Edit at your own risk! Be careful when regenerating this file
// as changes will be lost.

use super::units_of_work::CustomerUnitOfWorkFactory;
use super::{
    dtos::{CreateCustomerDto, CustomerDto},
    units_of_work::CustomerUnitOfWorkROFactory,
    use_cases::{
        create_customer_multi_uc::CreateCustomerMultiUseCase,
        create_customer_uc::CreateCustomerUseCase, get_customer_multi_uc::GetCustomerMultiUseCase,
        get_customer_uc::GetCustomerUseCase, remove_customer_multi_uc::RemoveCustomerMultiUseCase,
        remove_customer_uc::RemoveCustomerUseCase,
        update_customer_multi_uc::UpdateCustomerMultiUseCase,
        update_customer_uc::UpdateCustomerUseCase,
    },
};
use anyhow::{Ok, Result};

use common::undo_redo::UndoRedoManager;
use common::{database::db_context::DbContext, event::EventHub, types::EntityId};
use std::sync::Arc;

pub fn create(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    entity: &CreateCustomerDto,
) -> Result<CustomerDto> {
    let uow_factory = CustomerUnitOfWorkFactory::new(&db_context, &event_hub);
    let mut uc = CreateCustomerUseCase::new(Box::new(uow_factory));
    let result = uc.execute(entity.clone())?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(result)
}

pub fn get(db_context: &DbContext, id: &EntityId) -> Result<Option<CustomerDto>> {
    let uow_factory = CustomerUnitOfWorkROFactory::new(&db_context);
    let uc = GetCustomerUseCase::new(Box::new(uow_factory));
    uc.execute(id)
}

pub fn update(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    entity: &CustomerDto,
) -> Result<CustomerDto> {
    let uow_factory = CustomerUnitOfWorkFactory::new(&db_context, &event_hub);
    let mut uc = UpdateCustomerUseCase::new(Box::new(uow_factory));
    let result = uc.execute(entity)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(result)
}

pub fn remove(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    id: &EntityId,
) -> Result<()> {
    let uow_factory = CustomerUnitOfWorkFactory::new(&db_context, &event_hub);
    let mut uc = RemoveCustomerUseCase::new(Box::new(uow_factory));
    uc.execute(id)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(())
}

pub fn create_multi(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    entities: &[CreateCustomerDto],
) -> Result<Vec<CustomerDto>> {
    let uow_factory = CustomerUnitOfWorkFactory::new(&db_context, &event_hub);
    let mut uc = CreateCustomerMultiUseCase::new(Box::new(uow_factory));
    let result = uc.execute(entities)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(result)
}

pub fn get_multi(db_context: &DbContext, ids: &[EntityId]) -> Result<Vec<Option<CustomerDto>>> {
    let uow_factory = CustomerUnitOfWorkROFactory::new(&db_context);
    let uc = GetCustomerMultiUseCase::new(Box::new(uow_factory));
    uc.execute(ids)
}

pub fn update_multi(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    entities: &[CustomerDto],
) -> Result<Vec<CustomerDto>> {
    let uow_factory = CustomerUnitOfWorkFactory::new(&db_context, &event_hub);
    let mut uc = UpdateCustomerMultiUseCase::new(Box::new(uow_factory));
    let result = uc.execute(entities)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(result)
}

pub fn remove_multi(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let uow_factory = CustomerUnitOfWorkFactory::new(&db_context, &event_hub);
    let mut uc = RemoveCustomerMultiUseCase::new(Box::new(uow_factory));
    uc.execute(ids)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(())
}
