// Generated by Qleany. Edit at your own risk! Be careful when regenerating this file
// as changes will be lost.

use super::CustomerUnitOfWorkROFactoryTrait;
use crate::customer::dtos::CustomerDto;
use anyhow::Result;
use common::types::EntityId;

pub struct GetCustomerUseCase {
    uow_factory: Box<dyn CustomerUnitOfWorkROFactoryTrait>,
}

impl GetCustomerUseCase {
    pub fn new(uow_factory: Box<dyn CustomerUnitOfWorkROFactoryTrait>) -> Self {
        GetCustomerUseCase { uow_factory }
    }

    pub fn execute(&self, id: &EntityId) -> Result<Option<CustomerDto>> {
        let uow = self.uow_factory.create();
        uow.begin_transaction()?;
        let entity_option = uow.get_customer(&id)?;
        uow.end_transaction()?;

        Ok(entity_option.map(|entity| entity.into()))
    }
}
