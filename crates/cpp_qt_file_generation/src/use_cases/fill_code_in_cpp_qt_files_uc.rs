// Generated by Qleany. Edit at your own risk! Be careful when regenerating this file
// as changes will be lost.
use anyhow::{Result, anyhow};
use common::database::CommandUnitOfWork;
use common::entities::{File, System};
use common::long_operation::LongOperation;
use common::types::EntityId;
use std::path::PathBuf;
use std::sync::Arc;

pub trait FillCodeInCppQtFilesUnitOfWorkFactoryTrait: Send + Sync {
    fn create(&self) -> Box<dyn FillCodeInCppQtFilesUnitOfWorkTrait>;
}
//TODO: adapt entities and actions to real use : Create, CreateMulti, Get, GetMulti, Update, UpdateMulti, Delete,
//DeleteMulti, GetRO, GetMultiRO, GetRelationship, GetRelationshipRO, GetRelationshipsFromRightIds,
//GetRelationshipsFromRightIdsRO, SetRelationship, SetRelationshipMulti
//
// RO means Read Only.
// Do not mix read-only and write actions in the same unit of work.
//
// Exactly the same macros must be set in the use case uow trait file in ../units_of_work/fill_code_in_cpp_qt_files_uow.rs
//
#[macros::uow_action(entity = "System", action = "Get")]
#[macros::uow_action(entity = "System", action = "GetMulti")]
#[macros::uow_action(entity = "System", action = "Snapshot")]
#[macros::uow_action(entity = "System", action = "Restore")]
#[macros::uow_action(entity = "File", action = "Get")]
#[macros::uow_action(entity = "File", action = "GetMulti")]
#[macros::uow_action(entity = "File", action = "Snapshot")]
#[macros::uow_action(entity = "File", action = "Restore")]
pub trait FillCodeInCppQtFilesUnitOfWorkTrait: CommandUnitOfWork + Send + Sync {}

pub struct FillCodeInCppQtFilesUseCase {
    uow_factory: Box<dyn FillCodeInCppQtFilesUnitOfWorkFactoryTrait>,
}

impl FillCodeInCppQtFilesUseCase {
    pub fn new(uow_factory: Box<dyn FillCodeInCppQtFilesUnitOfWorkFactoryTrait>) -> Self {
        FillCodeInCppQtFilesUseCase { uow_factory }
    }
}
impl LongOperation for FillCodeInCppQtFilesUseCase {
    type Output = ();
    fn execute(
        &self,
        progress_callback: Box<dyn Fn(common::long_operation::OperationProgress) + Send>,
        cancel_flag: Arc<std::sync::atomic::AtomicBool>,
    ) -> Result<()> {
        use std::fs;
        use std::sync::atomic::Ordering;

        progress_callback(common::long_operation::OperationProgress::new(
            0.0,
            Some("Starting...".to_string()),
        ));

        let mut uow = self.uow_factory.create();
        uow.begin_transaction()?;

        //TODO: FillCodeInCppQtFilesUseCase to be implemented
        unimplemented!("FillCodeInCppQtFilesUseCase unimplemented");

        if cancel_flag.load(Ordering::Relaxed) {
            uow.rollback()?;
            return Err(anyhow!("Operation was cancelled"));
        }
        uow.commit()?;

        // Final progress
        progress_callback(common::long_operation::OperationProgress::new(
            100.0,
            Some("completed".to_string()),
        ));
        Ok(())
    }
}
