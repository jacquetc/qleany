// Generated by Qleany. Edit at your own risk! Be careful when regenerating this file
// as changes will be lost.

use crate::FillCppQtFilesDto;
use crate::FillCppQtFilesReturnDto;
use crate::GenerateCppQtCodeDto;
use crate::GenerateCppQtCodeReturnDto;
use crate::GenerateCppQtFilesDto;
use crate::GenerateCppQtFilesReturnDto;
use crate::GenerateCppQtPromptDto;
use crate::GenerateCppQtPromptReturnDto;
use crate::units_of_work::fill_code_in_cpp_qt_files_uow::FillCodeInCppQtFilesUnitOfWorkFactory;
use crate::units_of_work::fill_cpp_qt_files_uow::FillCppQtFilesUnitOfWorkFactory;
use crate::units_of_work::generate_cpp_qt_code_uow::GenerateCppQtCodeUnitOfWorkFactory;
use crate::units_of_work::generate_cpp_qt_files_uow::GenerateCppQtFilesUnitOfWorkFactory;
use crate::units_of_work::generate_cpp_qt_prompt_uow::GenerateCppQtPromptUnitOfWorkFactory;
use crate::use_cases::fill_code_in_cpp_qt_files_uc::FillCodeInCppQtFilesUseCase;
use crate::use_cases::fill_cpp_qt_files_uc::FillCppQtFilesUseCase;
use crate::use_cases::generate_cpp_qt_code_uc::GenerateCppQtCodeUseCase;
use crate::use_cases::generate_cpp_qt_files_uc::GenerateCppQtFilesUseCase;
use crate::use_cases::generate_cpp_qt_prompt_uc::GenerateCppQtPromptUseCase;
use anyhow::Result;
use common::event::{Event, Origin};

use common::event::CppQtFileGenerationEvent::FillCodeInCppQtFiles;
use common::event::CppQtFileGenerationEvent::FillCppQtFiles;
use common::event::CppQtFileGenerationEvent::GenerateCppQtCode;
use common::event::CppQtFileGenerationEvent::GenerateCppQtFiles;
use common::event::CppQtFileGenerationEvent::GenerateCppQtPrompt;

use common::long_operation::{LongOperationManager, OperationProgress};
use common::{database::db_context::DbContext, event::EventHub};
use std::sync::Arc;

pub fn fill_cpp_qt_files(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    dto: &FillCppQtFilesDto,
) -> Result<FillCppQtFilesReturnDto> {
    let uow_context = FillCppQtFilesUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = FillCppQtFilesUseCase::new(Box::new(uow_context));
    let return_dto = uc.execute(dto)?;
    // Notify that the handling manifest has been loaded
    event_hub.send_event(Event {
        origin: Origin::CppQtFileGeneration(FillCppQtFiles),
        ids: vec![],
        data: None,
    });
    Ok(return_dto)
}

pub fn generate_cpp_qt_code(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    dto: &GenerateCppQtCodeDto,
) -> Result<GenerateCppQtCodeReturnDto> {
    let uow_context = GenerateCppQtCodeUnitOfWorkFactory::new(db_context);
    let mut uc = GenerateCppQtCodeUseCase::new(Box::new(uow_context));
    let return_dto = uc.execute(dto)?;
    // Notify that the handling manifest has been loaded
    event_hub.send_event(Event {
        origin: Origin::CppQtFileGeneration(GenerateCppQtCode),
        ids: vec![],
        data: None,
    });
    Ok(return_dto)
}

pub fn generate_cpp_qt_files(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    long_operation_manager: &mut LongOperationManager,
    dto: &GenerateCppQtFilesDto,
) -> Result<String> {
    let uow_context = GenerateCppQtFilesUnitOfWorkFactory::new(db_context);
    let uc = GenerateCppQtFilesUseCase::new(Box::new(uow_context), dto);
    let operation_id = long_operation_manager.start_operation(uc);
    Ok(operation_id)
}

pub fn get_generate_cpp_qt_files_progress(
    long_operation_manager: &LongOperationManager,
    operation_id: &str,
) -> Option<OperationProgress> {
    long_operation_manager.get_operation_progress(operation_id)
}

pub fn get_generate_cpp_qt_files_result(
    long_operation_manager: &LongOperationManager,
    operation_id: &str,
) -> Result<Option<GenerateCppQtFilesReturnDto>> {
    // Get the operation result as a JSON string
    let result_json = long_operation_manager.get_operation_result(operation_id);

    // If there's no result, return None
    if result_json.is_none() {
        return Ok(None);
    }
    // Parse the JSON string into a GenerateCppQtFilesReturnDto
    let result_dto: GenerateCppQtFilesReturnDto = serde_json::from_str(&result_json.unwrap())?;

    Ok(Some(result_dto))
}

pub fn fill_code_in_cpp_qt_files(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    long_operation_manager: &mut LongOperationManager,
) -> Result<String> {
    let uow_context = FillCodeInCppQtFilesUnitOfWorkFactory::new(db_context, event_hub);
    let uc = FillCodeInCppQtFilesUseCase::new(Box::new(uow_context));
    let operation_id = long_operation_manager.start_operation(uc);
    Ok(operation_id)
}

pub fn get_fill_code_in_cpp_qt_files_progress(
    long_operation_manager: &LongOperationManager,
    operation_id: &str,
) -> Option<OperationProgress> {
    long_operation_manager.get_operation_progress(operation_id)
}

pub fn get_fill_code_in_cpp_qt_files_result(
    long_operation_manager: &LongOperationManager,
    operation_id: &str,
) -> Result<Option<()>> {
    // Get the operation result as a JSON string
    let result_json = long_operation_manager.get_operation_result(operation_id);

    // If there's no result, return None
    if result_json.is_none() {
        return Ok(None);
    }
    Ok(Some(()))
}

pub fn generate_cpp_qt_prompt(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    dto: &GenerateCppQtPromptDto,
) -> Result<GenerateCppQtPromptReturnDto> {
    let uow_context = GenerateCppQtPromptUnitOfWorkFactory::new(db_context);
    let mut uc = GenerateCppQtPromptUseCase::new(Box::new(uow_context));
    let return_dto = uc.execute(dto)?;
    // Notify that the handling manifest has been loaded
    event_hub.send_event(Event {
        origin: Origin::CppQtFileGeneration(GenerateCppQtPrompt),
        ids: vec![],
        data: None,
    });
    Ok(return_dto)
}
