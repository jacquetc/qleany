// Generated by Qleany. Edit at your own risk! Be careful when regenerating this file
// as changes will be lost.

use super::RootUnitOfWorkROFactoryTrait;
use crate::root::dtos::RootDto;
use anyhow::Result;

pub struct GetAllRootUseCase {
    uow_factory: Box<dyn RootUnitOfWorkROFactoryTrait>,
}

impl GetAllRootUseCase {
    pub fn new(uow_factory: Box<dyn RootUnitOfWorkROFactoryTrait>) -> Self {
        GetAllRootUseCase { uow_factory }
    }

    pub fn execute(&self) -> Result<Vec<RootDto>> {
        let uow = self.uow_factory.create();
        uow.begin_transaction()?;
        let entities = uow.get_all_root()?;
        uow.end_transaction()?;
        Ok(entities.into_iter().map(|entity| entity.into()).collect())
    }
}
