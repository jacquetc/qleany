// Generated by Qleany. Edit at your own risk! Be careful when regenerating this file
// as changes will be lost.

use super::SystemUnitOfWorkROFactoryTrait;
use crate::system::dtos::SystemDto;
use anyhow::Result;

pub struct GetAllSystemUseCase {
    uow_factory: Box<dyn SystemUnitOfWorkROFactoryTrait>,
}

impl GetAllSystemUseCase {
    pub fn new(uow_factory: Box<dyn SystemUnitOfWorkROFactoryTrait>) -> Self {
        GetAllSystemUseCase { uow_factory }
    }

    pub fn execute(&self) -> Result<Vec<SystemDto>> {
        let uow = self.uow_factory.create();
        uow.begin_transaction()?;
        let entities = uow.get_all_system()?;
        uow.end_transaction()?;
        Ok(entities.into_iter().map(|entity| entity.into()).collect())
    }
}
