// Generated by Qleany. Edit at your own risk! Be careful when regenerating this file
// as changes will be lost.

use super::units_of_work::DtoUnitOfWorkFactory;
use super::{
    dtos::{CreateDtoDto, DtoDto},
    units_of_work::DtoUnitOfWorkROFactory,
    use_cases::{
        create_dto_multi_uc::CreateDtoMultiUseCase, create_dto_uc::CreateDtoUseCase,
        create_orphans_dto_multi_uc::CreateOrphansDtoMultiUseCase,
        create_orphans_dto_uc::CreateOrphansDtoUseCase, get_dto_multi_uc::GetDtoMultiUseCase,
        get_dto_uc::GetDtoUseCase, remove_dto_multi_uc::RemoveDtoMultiUseCase,
        remove_dto_uc::RemoveDtoUseCase, update_dto_multi_uc::UpdateDtoMultiUseCase,
        update_dto_uc::UpdateDtoUseCase,
    },
};
use crate::DtoRelationshipDto;
use crate::dto::use_cases::get_dto_relationship_uc::GetDtoRelationshipUseCase;
use crate::dto::use_cases::set_dto_relationship_uc::SetDtoRelationshipUseCase;
use anyhow::{Ok, Result};
use common::direct_access::dto::DtoRelationshipField;
use common::undo_redo::UndoRedoManager;
use common::{database::db_context::DbContext, event::EventHub, types::EntityId};
use std::sync::Arc;

pub fn create_orphans(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    entity: &CreateDtoDto,
) -> Result<DtoDto> {
    let uow_factory = DtoUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = CreateOrphansDtoUseCase::new(Box::new(uow_factory));
    let result = uc.execute(entity.clone())?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(result)
}
pub fn create(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    entity: &CreateDtoDto,
    owner_id: EntityId,
    index: i32,
) -> Result<DtoDto> {
    let uow_factory = DtoUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = CreateDtoUseCase::new(Box::new(uow_factory));
    let result = uc.execute(entity.clone(), owner_id, index)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(result)
}
pub fn get(db_context: &DbContext, id: &EntityId) -> Result<Option<DtoDto>> {
    let uow_factory = DtoUnitOfWorkROFactory::new(db_context);
    let uc = GetDtoUseCase::new(Box::new(uow_factory));
    uc.execute(id)
}

pub fn update(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    entity: &DtoDto,
) -> Result<DtoDto> {
    let uow_factory = DtoUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = UpdateDtoUseCase::new(Box::new(uow_factory));
    let result = uc.execute(entity)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(result)
}

pub fn remove(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    id: &EntityId,
) -> Result<()> {
    let uow_factory = DtoUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = RemoveDtoUseCase::new(Box::new(uow_factory));
    uc.execute(id)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(())
}

pub fn create_orphans_multi(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    entities: &[CreateDtoDto],
) -> Result<Vec<DtoDto>> {
    let uow_factory = DtoUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = CreateOrphansDtoMultiUseCase::new(Box::new(uow_factory));
    let result = uc.execute(entities)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(result)
}
pub fn create_multi(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    entities: &[CreateDtoDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<DtoDto>> {
    let uow_factory = DtoUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = CreateDtoMultiUseCase::new(Box::new(uow_factory));
    let result = uc.execute(entities, owner_id, index)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(result)
}
pub fn get_multi(db_context: &DbContext, ids: &[EntityId]) -> Result<Vec<Option<DtoDto>>> {
    let uow_factory = DtoUnitOfWorkROFactory::new(db_context);
    let uc = GetDtoMultiUseCase::new(Box::new(uow_factory));
    uc.execute(ids)
}

pub fn update_multi(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    entities: &[DtoDto],
) -> Result<Vec<DtoDto>> {
    let uow_factory = DtoUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = UpdateDtoMultiUseCase::new(Box::new(uow_factory));
    let result = uc.execute(entities)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(result)
}

pub fn remove_multi(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let uow_factory = DtoUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = RemoveDtoMultiUseCase::new(Box::new(uow_factory));
    uc.execute(ids)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(())
}

pub fn get_relationship(
    db_context: &DbContext,
    id: &EntityId,
    field: &DtoRelationshipField,
) -> Result<Vec<EntityId>> {
    let uow_factory = DtoUnitOfWorkROFactory::new(db_context);
    let uc = GetDtoRelationshipUseCase::new(Box::new(uow_factory));
    uc.execute(id, field)
}

pub fn set_relationship(
    db_context: &DbContext,
    event_hub: &Arc<EventHub>,
    undo_redo_manager: &mut UndoRedoManager,
    stack_id: Option<u64>,
    dto: &DtoRelationshipDto,
) -> Result<()> {
    let uow_factory = DtoUnitOfWorkFactory::new(db_context, event_hub);
    let mut uc = SetDtoRelationshipUseCase::new(Box::new(uow_factory));
    uc.execute(dto)?;
    undo_redo_manager.add_command_to_stack(Box::new(uc), stack_id)?;
    Ok(())
}
