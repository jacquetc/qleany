// Auto-generated by qleany. Do not edit by hand.

use super::FeatureUnitOfWorkFactoryTrait;
use crate::feature::dtos::FeatureDto;
use anyhow::{Ok, Result};
use common::{entities::Feature, undo_redo::UndoRedoCommand};
use std::any::Any;
use std::collections::VecDeque;

pub struct UpdateFeatureUseCase {
    uow_factory: Box<dyn FeatureUnitOfWorkFactoryTrait>,
    undo_stack: VecDeque<Feature>,
    redo_stack: VecDeque<Feature>,
}

impl UpdateFeatureUseCase {
    pub fn new(uow_factory: Box<dyn FeatureUnitOfWorkFactoryTrait>) -> Self {
        UpdateFeatureUseCase {
            uow_factory,
            undo_stack: VecDeque::new(),
            redo_stack: VecDeque::new(),
        }
    }

    pub fn description(&self) -> &str {
        "Update Feature"
    }

    pub fn execute(&mut self, dto: &FeatureDto) -> Result<FeatureDto> {
        let mut uow = self.uow_factory.create();
        uow.begin_transaction()?;
        // check if id exists
        if uow.get_feature(&dto.id)?.is_none() {
            return Err(anyhow::anyhow!("Feature with id {} does not exist", dto.id));
        }
        // store in undo stack
        let entity = uow.get_feature(&dto.id)?.unwrap();
        self.undo_stack.push_back(entity.clone());

        let entity = uow.update_feature(&dto.into())?;
        uow.commit()?;

        Ok(entity.into())
    }
}

impl UndoRedoCommand for UpdateFeatureUseCase {
    fn undo(&mut self) -> Result<()> {
        if let Some(last_entity) = self.undo_stack.pop_back() {
            let mut uow = self.uow_factory.create();
            uow.begin_transaction()?;
            uow.update_feature(&last_entity)?;
            uow.commit()?;
            self.redo_stack.push_back(last_entity);
        }
        Ok(())
    }

    fn redo(&mut self) -> Result<()> {
        if let Some(entity) = self.redo_stack.pop_back() {
            let mut uow = self.uow_factory.create();
            uow.begin_transaction()?;
            uow.update_feature(&entity)?;
            uow.commit()?;
            self.undo_stack.push_back(entity);
        }
        Ok(())
    }
    fn as_any(&self) -> &dyn Any {
        self
    }
}
