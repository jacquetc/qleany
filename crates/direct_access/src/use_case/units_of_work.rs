// Generated by Qleany v0.0.1 from entity_units_of_work.tera

use anyhow::{Ok, Result};
use common::database::{CommandUnitOfWork, QueryUnitOfWork};
use common::database::{db_context::DbContext, transactions::Transaction};
use common::direct_access::repository_factory;
use common::direct_access::use_case::UseCaseRelationshipField;
use common::direct_access::use_cases;
use common::entities::UseCase;
use common::event::{AllEvent, DirectAccessEntity, Event, EventBuffer, EventHub, Origin};
use common::snapshot::EntityTreeSnapshot;
use common::types;
use common::types::EntityId;
use std::cell::RefCell;
use std::sync::Arc;

// ===========================================================================
// Write UoW
// ===========================================================================

pub struct UseCaseWriteUoW {
    context: DbContext,
    transaction: Option<Transaction>,
    event_hub: Arc<EventHub>,
    event_buffer: RefCell<EventBuffer>,
}

impl UseCaseWriteUoW {
    pub fn new(db_context: &DbContext, event_hub: &Arc<EventHub>) -> Self {
        UseCaseWriteUoW {
            context: db_context.clone(),
            transaction: None,
            event_hub: event_hub.clone(),
            event_buffer: RefCell::new(EventBuffer::new()),
        }
    }
}

impl CommandUnitOfWork for UseCaseWriteUoW {
    fn begin_transaction(&mut self) -> Result<()> {
        self.transaction = Some(Transaction::begin_write_transaction(&self.context)?);
        self.event_buffer.get_mut().begin_buffering();
        Ok(())
    }

    fn commit(&mut self) -> Result<()> {
        self.transaction
            .take()
            .ok_or_else(|| anyhow::anyhow!("No active transaction"))?
            .commit()?;
        for event in self.event_buffer.get_mut().flush() {
            self.event_hub.send_event(event);
        }
        Ok(())
    }

    fn rollback(&mut self) -> Result<()> {
        self.transaction
            .take()
            .ok_or_else(|| anyhow::anyhow!("No active transaction"))?
            .rollback()?;
        self.event_buffer.get_mut().discard();
        Ok(())
    }

    fn create_savepoint(&self) -> Result<types::Savepoint> {
        self.transaction
            .as_ref()
            .ok_or_else(|| anyhow::anyhow!("No active transaction"))?
            .create_savepoint()
    }

    fn restore_to_savepoint(&mut self, savepoint: types::Savepoint) -> Result<()> {
        let mut transaction = self
            .transaction
            .take()
            .ok_or_else(|| anyhow::anyhow!("No active transaction"))?;
        transaction.restore_to_savepoint(savepoint)?;

        // Discard buffered events — savepoint restore invalidated them
        self.event_buffer.get_mut().discard();

        // Send Reset immediately (not buffered — UI must refresh now)
        self.event_hub.send_event(Event {
            origin: Origin::DirectAccess(DirectAccessEntity::All(AllEvent::Reset)),
            ids: vec![],
            data: None,
        });

        // Recreate the transaction after restoring to savepoint
        self.transaction = Some(transaction);

        Ok(())
    }
}

impl use_cases::WriteUoW for UseCaseWriteUoW {
    type Entity = UseCase;

    fn get(&self, id: &EntityId) -> Result<Option<UseCase>> {
        let transaction = self.transaction.as_ref().expect("Transaction not started");
        let repo = repository_factory::write::create_use_case_repository(transaction)?;
        Ok(repo.get(id)?)
    }

    fn get_multi(&self, ids: &[EntityId]) -> Result<Vec<Option<UseCase>>> {
        let transaction = self.transaction.as_ref().expect("Transaction not started");
        let repo = repository_factory::write::create_use_case_repository(transaction)?;
        Ok(repo.get_multi(ids)?)
    }

    fn get_all(&self) -> Result<Vec<UseCase>> {
        let transaction = self.transaction.as_ref().expect("Transaction not started");
        let repo = repository_factory::write::create_use_case_repository(transaction)?;
        Ok(repo.get_all()?)
    }

    fn create_orphan_multi(&self, entities: &[UseCase]) -> Result<Vec<UseCase>> {
        let transaction = self.transaction.as_ref().expect("Transaction not started");
        let mut repo = repository_factory::write::create_use_case_repository(transaction)?;
        let mut event_buffer = self.event_buffer.borrow_mut();
        Ok(repo.create_orphan_multi(&mut event_buffer, entities)?)
    }

    fn update_multi(&self, entities: &[UseCase]) -> Result<Vec<UseCase>> {
        let transaction = self.transaction.as_ref().expect("Transaction not started");
        let mut repo = repository_factory::write::create_use_case_repository(transaction)?;
        let mut event_buffer = self.event_buffer.borrow_mut();
        Ok(repo.update_multi(&mut event_buffer, entities)?)
    }

    fn remove(&self, id: &EntityId) -> Result<()> {
        let transaction = self.transaction.as_ref().expect("Transaction not started");
        let mut repo = repository_factory::write::create_use_case_repository(transaction)?;
        let mut event_buffer = self.event_buffer.borrow_mut();
        Ok(repo.remove(&mut event_buffer, id)?)
    }

    fn remove_multi(&self, ids: &[EntityId]) -> Result<()> {
        let transaction = self.transaction.as_ref().expect("Transaction not started");
        let mut repo = repository_factory::write::create_use_case_repository(transaction)?;
        let mut event_buffer = self.event_buffer.borrow_mut();
        Ok(repo.remove_multi(&mut event_buffer, ids)?)
    }

    fn snapshot(&self, ids: &[EntityId]) -> Result<EntityTreeSnapshot> {
        let transaction = self.transaction.as_ref().expect("Transaction not started");
        let repo = repository_factory::write::create_use_case_repository(transaction)?;
        Ok(repo.snapshot(ids)?)
    }

    fn restore(&self, snap: &EntityTreeSnapshot) -> Result<()> {
        let transaction = self.transaction.as_ref().expect("Transaction not started");
        let mut repo = repository_factory::write::create_use_case_repository(transaction)?;
        let mut event_buffer = self.event_buffer.borrow_mut();
        Ok(repo.restore(&mut event_buffer, snap)?)
    }
}

impl use_cases::OwnedWriteUoW for UseCaseWriteUoW {
    fn create_multi(
        &self,
        entities: &[UseCase],
        owner_id: EntityId,
        index: i32,
    ) -> Result<Vec<UseCase>> {
        let transaction = self.transaction.as_ref().expect("Transaction not started");
        let mut repo = repository_factory::write::create_use_case_repository(transaction)?;
        let mut event_buffer = self.event_buffer.borrow_mut();
        Ok(repo.create_multi(&mut event_buffer, entities, owner_id, index)?)
    }

    fn get_relationships_from_owner(&self, owner_id: &EntityId) -> Result<Vec<EntityId>> {
        let transaction = self.transaction.as_ref().expect("Transaction not started");
        let repo = repository_factory::write::create_use_case_repository(transaction)?;
        Ok(repo.get_relationships_from_owner(owner_id)?)
    }

    fn set_relationships_in_owner(&self, owner_id: &EntityId, ids: &[EntityId]) -> Result<()> {
        let transaction = self.transaction.as_ref().expect("Transaction not started");
        let mut repo = repository_factory::write::create_use_case_repository(transaction)?;
        let mut event_buffer = self.event_buffer.borrow_mut();
        repo.set_relationships_in_owner(&mut event_buffer, owner_id, ids)?;
        Ok(())
    }
}

impl use_cases::WriteRelUoW<UseCaseRelationshipField> for UseCaseWriteUoW {
    fn set_relationship(
        &self,
        id: &EntityId,
        field: &UseCaseRelationshipField,
        right_ids: &[EntityId],
    ) -> Result<()> {
        let transaction = self.transaction.as_ref().expect("Transaction not started");
        let mut repo = repository_factory::write::create_use_case_repository(transaction)?;
        let mut event_buffer = self.event_buffer.borrow_mut();
        repo.set_relationship(&mut event_buffer, id, field, right_ids)?;
        Ok(())
    }
}

// ---------------------------------------------------------------------------
// Write UoW Factory
// ---------------------------------------------------------------------------

pub struct UseCaseWriteUoWFactory {
    context: DbContext,
    event_hub: Arc<EventHub>,
}

impl UseCaseWriteUoWFactory {
    pub fn new(db_context: &DbContext, event_hub: &Arc<EventHub>) -> Self {
        UseCaseWriteUoWFactory {
            context: db_context.clone(),
            event_hub: event_hub.clone(),
        }
    }
}

impl use_cases::WriteUoWFactory for UseCaseWriteUoWFactory {
    type Entity = UseCase;
    fn create(&self) -> Box<dyn use_cases::WriteUoW<Entity = UseCase>> {
        Box::new(UseCaseWriteUoW::new(&self.context, &self.event_hub))
    }
}

impl use_cases::OwnedWriteUoWFactory for UseCaseWriteUoWFactory {
    type Entity = UseCase;
    fn create(&self) -> Box<dyn use_cases::OwnedWriteUoW<Entity = UseCase>> {
        Box::new(UseCaseWriteUoW::new(&self.context, &self.event_hub))
    }
}

impl use_cases::WriteRelUoWFactory<UseCaseRelationshipField> for UseCaseWriteUoWFactory {
    fn create(&self) -> Box<dyn use_cases::WriteRelUoW<UseCaseRelationshipField>> {
        Box::new(UseCaseWriteUoW::new(&self.context, &self.event_hub))
    }
}

// ===========================================================================
// Read-Only UoW
// ===========================================================================

pub struct UseCaseReadUoW {
    context: DbContext,
    transaction: RefCell<Option<Transaction>>,
}

impl UseCaseReadUoW {
    pub fn new(db_context: &DbContext) -> Self {
        UseCaseReadUoW {
            context: db_context.clone(),
            transaction: RefCell::new(None),
        }
    }
}

impl QueryUnitOfWork for UseCaseReadUoW {
    fn begin_transaction(&self) -> Result<()> {
        self.transaction
            .replace(Some(Transaction::begin_read_transaction(&self.context)?));
        Ok(())
    }

    fn end_transaction(&self) -> Result<()> {
        self.transaction
            .take()
            .ok_or_else(|| anyhow::anyhow!("No active transaction"))?
            .end_read_transaction()?;
        Ok(())
    }
}

impl use_cases::ReadUoW for UseCaseReadUoW {
    type Entity = UseCase;

    fn get(&self, id: &EntityId) -> Result<Option<UseCase>> {
        let transaction = self.transaction.borrow();
        let repo = repository_factory::read::create_use_case_repository(
            transaction
                .as_ref()
                .ok_or_else(|| anyhow::anyhow!("No active transaction"))?,
        )?;
        Ok(repo.get(id)?)
    }

    fn get_multi(&self, ids: &[EntityId]) -> Result<Vec<Option<UseCase>>> {
        let transaction = self.transaction.borrow();
        let repo = repository_factory::read::create_use_case_repository(
            transaction
                .as_ref()
                .ok_or_else(|| anyhow::anyhow!("No active transaction"))?,
        )?;
        Ok(repo.get_multi(ids)?)
    }

    fn get_all(&self) -> Result<Vec<UseCase>> {
        let transaction = self.transaction.borrow();
        let repo = repository_factory::read::create_use_case_repository(
            transaction
                .as_ref()
                .ok_or_else(|| anyhow::anyhow!("No active transaction"))?,
        )?;
        Ok(repo.get_all()?)
    }
}

impl use_cases::ReadRelUoW<UseCaseRelationshipField> for UseCaseReadUoW {
    fn get_relationship(
        &self,
        id: &EntityId,
        field: &UseCaseRelationshipField,
    ) -> Result<Vec<EntityId>> {
        let transaction = self.transaction.borrow();
        let repo = repository_factory::read::create_use_case_repository(
            transaction
                .as_ref()
                .ok_or_else(|| anyhow::anyhow!("No active transaction"))?,
        )?;
        Ok(repo.get_relationship(id, field)?)
    }

    fn get_relationship_many(
        &self,
        ids: &[EntityId],
        field: &UseCaseRelationshipField,
    ) -> Result<std::collections::HashMap<EntityId, Vec<EntityId>>> {
        let transaction = self.transaction.borrow();
        let repo = repository_factory::read::create_use_case_repository(
            transaction
                .as_ref()
                .ok_or_else(|| anyhow::anyhow!("No active transaction"))?,
        )?;
        Ok(repo.get_relationship_many(ids, field)?)
    }

    fn get_relationship_count(
        &self,
        id: &EntityId,
        field: &UseCaseRelationshipField,
    ) -> Result<usize> {
        let transaction = self.transaction.borrow();
        let repo = repository_factory::read::create_use_case_repository(
            transaction
                .as_ref()
                .ok_or_else(|| anyhow::anyhow!("No active transaction"))?,
        )?;
        Ok(repo.get_relationship_count(id, field)?)
    }

    fn get_relationship_in_range(
        &self,
        id: &EntityId,
        field: &UseCaseRelationshipField,
        offset: usize,
        limit: usize,
    ) -> Result<Vec<EntityId>> {
        let transaction = self.transaction.borrow();
        let repo = repository_factory::read::create_use_case_repository(
            transaction
                .as_ref()
                .ok_or_else(|| anyhow::anyhow!("No active transaction"))?,
        )?;
        Ok(repo.get_relationship_in_range(id, field, offset, limit)?)
    }
}

// ---------------------------------------------------------------------------
// Read-Only UoW Factory
// ---------------------------------------------------------------------------

pub struct UseCaseReadUoWFactory {
    context: DbContext,
}

impl UseCaseReadUoWFactory {
    pub fn new(db_context: &DbContext) -> Self {
        UseCaseReadUoWFactory {
            context: db_context.clone(),
        }
    }
}

impl use_cases::ReadUoWFactory for UseCaseReadUoWFactory {
    type Entity = UseCase;
    fn create(&self) -> Box<dyn use_cases::ReadUoW<Entity = UseCase>> {
        Box::new(UseCaseReadUoW::new(&self.context))
    }
}

impl use_cases::ReadRelUoWFactory<UseCaseRelationshipField> for UseCaseReadUoWFactory {
    fn create(&self) -> Box<dyn use_cases::ReadRelUoW<UseCaseRelationshipField>> {
        Box::new(UseCaseReadUoW::new(&self.context))
    }
}
