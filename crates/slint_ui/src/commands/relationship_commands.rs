// Generated by Qleany. Edit at your own risk! Be careful when regenerating this file
// as changes will be lost.


//! Relationship entity commands for Slint UI

use crate::app_context::AppContext;
use common::direct_access::relationship::RelationshipRelationshipField;
use direct_access::RelationshipRelationshipDto;
use direct_access::{CreateRelationshipDto, RelationshipDto, relationship_controller};
use common::types::EntityId;

/// Create a new relationship entity (orphan, no parent)
pub fn create_orphans_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateRelationshipDto,
) -> Result<RelationshipDto, String> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    relationship_controller::create_orphans(
        &ctx.db_context,
        &ctx.event_hub,
        &mut *undo_redo_manager,
        stack_id,
        dto,
    )
    .map_err(|e| format!("Error creating relationship: {:?}", e))
}/// Create a new relationship entity as child of owner
pub fn create_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateRelationshipDto,
    owner_id: EntityId,
    index: i32,
) -> Result<RelationshipDto, String> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    relationship_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut *undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .map_err(|e| format!("Error creating relationship: {:?}", e))
}/// Create multiple relationship entities (orphan, no parent)
pub fn create_orphans_relationship_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateRelationshipDto],
) -> Result<Vec<RelationshipDto>, String> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    relationship_controller::create_orphans_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut *undo_redo_manager,
        stack_id,
        dtos,
    )
    .map_err(|e| format!("Error creating entities: {:?}", e))
}/// Create multiple relationship entities as children of owner
pub fn create_relationship_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateRelationshipDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<RelationshipDto>, String> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    relationship_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut *undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .map_err(|e| format!("Error creating entities: {:?}", e))
}/// Get a relationship entity by ID
pub fn get_relationship(ctx: &AppContext, id: &EntityId) -> Result<Option<RelationshipDto>, String> {
    relationship_controller::get(&ctx.db_context, id).map_err(|e| format!("Error getting relationship: {:?}", e))
}

/// Get multiple relationship entities by IDs
pub fn get_relationship_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<RelationshipDto>>, String> {
    relationship_controller::get_multi(&ctx.db_context, ids)
        .map_err(|e| format!("Error getting entities: {:?}", e))
}

/// Update a relationship entity
pub fn update_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &RelationshipDto,
) -> Result<RelationshipDto, String> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    relationship_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut *undo_redo_manager,
        stack_id,
        dto,
    )
    .map_err(|e| format!("Error updating relationship: {:?}", e))
}

/// Update multiple relationship entities
pub fn update_relationship_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[RelationshipDto],
) -> Result<Vec<RelationshipDto>, String> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    relationship_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut *undo_redo_manager,
        stack_id,
        dtos,
    )
    .map_err(|e| format!("Error updating entities: {:?}", e))
}

/// Remove a relationship entity by ID
pub fn remove_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    id: &EntityId,
) -> Result<(), String> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    let result = relationship_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut *undo_redo_manager,
        stack_id,
        id,
    )
    .map_err(|e| format!("Error deleting relationship: {:?}", e));

    undo_redo_manager.clear_all_stacks();
    result
    
}

/// Remove multiple relationship entities by IDs
pub fn remove_relationship_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<(), String> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    let result = relationship_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut *undo_redo_manager,
        stack_id,
        ids,
    )
    .map_err(|e| format!("Error deleting relationship: {:?}", e));

    undo_redo_manager.clear_all_stacks();
    result
    
}

/// Get a relationship relationship
pub fn get_relationship_relationship(
    ctx: &AppContext,
    id: &EntityId,
    field: &RelationshipRelationshipField,
) -> Result<Vec<EntityId>, String> {
    relationship_controller::get_relationship(&ctx.db_context, id, field)
        .map_err(|e| format!("Error getting relationship relationship: {:?}", e))
}

/// Set a relationship relationship
pub fn set_relationship_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &RelationshipRelationshipDto,
) -> Result<(), String> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    relationship_controller::set_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut *undo_redo_manager,
        stack_id,
        dto,
    )
    
    .map_err(|e| format!("Error setting relationship relationship: {:?}", e))
}




