// Generated by Qleany v1.7.0 from frontend_entity_commands.tera

//! Feature entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::direct_access::feature::FeatureRelationshipField;
use common::types::EntityId;
use direct_access::FeatureRelationshipDto;
use direct_access::{CreateFeatureDto, FeatureDto, UpdateFeatureDto, feature_controller};

/// Create a new feature entity (orphan, no parent)
pub fn create_orphan_feature(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateFeatureDto,
) -> Result<FeatureDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    feature_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating feature")
}
/// Create a new feature entity as child of owner
pub fn create_feature(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateFeatureDto,
    owner_id: EntityId,
    index: i32,
) -> Result<FeatureDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    feature_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating feature")
}
/// Create multiple feature entities (orphan, no parent)
pub fn create_orphan_feature_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateFeatureDto],
) -> Result<Vec<FeatureDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    feature_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating feature entities")
}
/// Create multiple feature entities as children of owner
pub fn create_feature_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateFeatureDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<FeatureDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    feature_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating feature entities")
}
/// Get a feature entity by ID
pub fn get_feature(ctx: &AppContext, id: &EntityId) -> Result<Option<FeatureDto>> {
    feature_controller::get(&ctx.db_context, id).context("getting feature")
}

/// Get multiple feature entities by IDs
pub fn get_feature_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<FeatureDto>>> {
    feature_controller::get_multi(&ctx.db_context, ids).context("getting feature entities")
}

/// Get all feature entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_feature(ctx: &AppContext) -> Result<Vec<FeatureDto>> {
    feature_controller::get_all(&ctx.db_context).context("getting all feature entities")
}

/// Update a feature entity
pub fn update_feature(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdateFeatureDto,
) -> Result<FeatureDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    feature_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating feature")
}

/// Update multiple feature entities
pub fn update_feature_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateFeatureDto],
) -> Result<Vec<FeatureDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    feature_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating feature entities")
}

/// Update a feature entity with relationships
pub fn update_feature_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &FeatureDto,
) -> Result<FeatureDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    feature_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating feature with relationships")
}

/// Update multiple feature entities with relationships
pub fn update_feature_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[FeatureDto],
) -> Result<Vec<FeatureDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    feature_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating feature entities with relationships")
}

/// Remove a feature entity by ID
pub fn remove_feature(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    feature_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing feature")
}

/// Remove multiple feature entities by IDs
pub fn remove_feature_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    feature_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing feature entities")
}

/// Get a feature relationship
pub fn get_feature_relationship(
    ctx: &AppContext,
    id: &EntityId,
    field: &FeatureRelationshipField,
) -> Result<Vec<EntityId>> {
    feature_controller::get_relationship(&ctx.db_context, id, field)
        .context("getting feature relationship")
}

/// Get relationship IDs for multiple feature entities at once
pub fn get_feature_relationship_many(
    ctx: &AppContext,
    ids: &[EntityId],
    field: &FeatureRelationshipField,
) -> Result<std::collections::HashMap<EntityId, Vec<EntityId>>> {
    feature_controller::get_relationship_many(&ctx.db_context, ids, field)
        .context("getting feature relationships (many)")
}

/// Get relationship count for a feature entity
pub fn get_feature_relationship_count(
    ctx: &AppContext,
    id: &EntityId,
    field: &FeatureRelationshipField,
) -> Result<usize> {
    feature_controller::get_relationship_count(&ctx.db_context, id, field)
        .context("getting feature relationship count")
}

/// Get relationship IDs for a feature entity with pagination
pub fn get_feature_relationship_in_range(
    ctx: &AppContext,
    id: &EntityId,
    field: &FeatureRelationshipField,
    offset: usize,
    limit: usize,
) -> Result<Vec<EntityId>> {
    feature_controller::get_relationship_in_range(&ctx.db_context, id, field, offset, limit)
        .context("getting feature relationship range")
}

/// Set a feature relationship
pub fn set_feature_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &FeatureRelationshipDto,
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    feature_controller::set_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("setting feature relationship")
}

/// Move (reorder) IDs within a feature relationship
pub fn move_feature_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    id: &EntityId,
    field: &FeatureRelationshipField,
    ids_to_move: &[EntityId],
    new_index: i32,
) -> Result<Vec<EntityId>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    feature_controller::move_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
        field,
        ids_to_move,
        new_index,
    )
    .context("moving feature relationship")
}
