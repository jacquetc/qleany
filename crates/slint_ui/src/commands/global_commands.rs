// Generated by Qleany v1.7.0 from frontend_entity_commands.tera

//! Global entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::types::EntityId;
use direct_access::{CreateGlobalDto, GlobalDto, UpdateGlobalDto, global_controller};

/// Create a new global entity (orphan, no parent)
pub fn create_orphan_global(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateGlobalDto,
) -> Result<GlobalDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    global_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating global")
}
/// Create a new global entity as child of owner
pub fn create_global(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateGlobalDto,
    owner_id: EntityId,
    index: i32,
) -> Result<GlobalDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    global_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating global")
}
/// Create multiple global entities (orphan, no parent)
pub fn create_orphan_global_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateGlobalDto],
) -> Result<Vec<GlobalDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    global_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating global entities")
}
/// Create multiple global entities as children of owner
pub fn create_global_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateGlobalDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<GlobalDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    global_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating global entities")
}
/// Get a global entity by ID
pub fn get_global(ctx: &AppContext, id: &EntityId) -> Result<Option<GlobalDto>> {
    global_controller::get(&ctx.db_context, id).context("getting global")
}

/// Get multiple global entities by IDs
pub fn get_global_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<GlobalDto>>> {
    global_controller::get_multi(&ctx.db_context, ids).context("getting global entities")
}

/// Get all global entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_global(ctx: &AppContext) -> Result<Vec<GlobalDto>> {
    global_controller::get_all(&ctx.db_context).context("getting all global entities")
}

/// Update a global entity
pub fn update_global(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdateGlobalDto,
) -> Result<GlobalDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    global_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating global")
}

/// Update multiple global entities
pub fn update_global_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateGlobalDto],
) -> Result<Vec<GlobalDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    global_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating global entities")
}

/// Update a global entity with relationships
pub fn update_global_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &GlobalDto,
) -> Result<GlobalDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    global_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating global with relationships")
}

/// Update multiple global entities with relationships
pub fn update_global_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[GlobalDto],
) -> Result<Vec<GlobalDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    global_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating global entities with relationships")
}

/// Remove a global entity by ID
pub fn remove_global(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    global_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing global")
}

/// Remove multiple global entities by IDs
pub fn remove_global_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    global_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing global entities")
}
