// Generated by Qleany v1.7.0 from frontend_entity_commands.tera

//! Entity entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::direct_access::entity::EntityRelationshipField;
use common::types::EntityId;
use direct_access::EntityRelationshipDto;
use direct_access::{CreateEntityDto, EntityDto, UpdateEntityDto, entity_controller};

/// Create a new entity entity (orphan, no parent)
pub fn create_orphan_entity(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateEntityDto,
) -> Result<EntityDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    entity_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating entity")
}
/// Create a new entity entity as child of owner
pub fn create_entity(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateEntityDto,
    owner_id: EntityId,
    index: i32,
) -> Result<EntityDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    entity_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating entity")
}
/// Create multiple entity entities (orphan, no parent)
pub fn create_orphan_entity_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateEntityDto],
) -> Result<Vec<EntityDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    entity_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating entity entities")
}
/// Create multiple entity entities as children of owner
pub fn create_entity_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateEntityDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<EntityDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    entity_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating entity entities")
}
/// Get a entity entity by ID
pub fn get_entity(ctx: &AppContext, id: &EntityId) -> Result<Option<EntityDto>> {
    entity_controller::get(&ctx.db_context, id).context("getting entity")
}

/// Get multiple entity entities by IDs
pub fn get_entity_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<EntityDto>>> {
    entity_controller::get_multi(&ctx.db_context, ids).context("getting entity entities")
}

/// Get all entity entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_entity(ctx: &AppContext) -> Result<Vec<EntityDto>> {
    entity_controller::get_all(&ctx.db_context).context("getting all entity entities")
}

/// Update a entity entity
pub fn update_entity(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &UpdateEntityDto,
) -> Result<EntityDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    entity_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating entity")
}

/// Update multiple entity entities
pub fn update_entity_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateEntityDto],
) -> Result<Vec<EntityDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    entity_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating entity entities")
}

/// Update a entity entity with relationships
pub fn update_entity_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &EntityDto,
) -> Result<EntityDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    entity_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating entity with relationships")
}

/// Update multiple entity entities with relationships
pub fn update_entity_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[EntityDto],
) -> Result<Vec<EntityDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    entity_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating entity entities with relationships")
}

/// Remove a entity entity by ID
pub fn remove_entity(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    entity_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing entity")
}

/// Remove multiple entity entities by IDs
pub fn remove_entity_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    entity_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing entity entities")
}

/// Get a entity relationship
pub fn get_entity_relationship(
    ctx: &AppContext,
    id: &EntityId,
    field: &EntityRelationshipField,
) -> Result<Vec<EntityId>> {
    entity_controller::get_relationship(&ctx.db_context, id, field)
        .context("getting entity relationship")
}

/// Get relationship IDs for multiple entity entities at once
pub fn get_entity_relationship_many(
    ctx: &AppContext,
    ids: &[EntityId],
    field: &EntityRelationshipField,
) -> Result<std::collections::HashMap<EntityId, Vec<EntityId>>> {
    entity_controller::get_relationship_many(&ctx.db_context, ids, field)
        .context("getting entity relationships (many)")
}

/// Get relationship count for a entity entity
pub fn get_entity_relationship_count(
    ctx: &AppContext,
    id: &EntityId,
    field: &EntityRelationshipField,
) -> Result<usize> {
    entity_controller::get_relationship_count(&ctx.db_context, id, field)
        .context("getting entity relationship count")
}

/// Get relationship IDs for a entity entity with pagination
pub fn get_entity_relationship_in_range(
    ctx: &AppContext,
    id: &EntityId,
    field: &EntityRelationshipField,
    offset: usize,
    limit: usize,
) -> Result<Vec<EntityId>> {
    entity_controller::get_relationship_in_range(&ctx.db_context, id, field, offset, limit)
        .context("getting entity relationship range")
}

/// Set a entity relationship
pub fn set_entity_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &EntityRelationshipDto,
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    entity_controller::set_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("setting entity relationship")
}

/// Move (reorder) IDs within a entity relationship
pub fn move_entity_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    id: &EntityId,
    field: &EntityRelationshipField,
    ids_to_move: &[EntityId],
    new_index: i32,
) -> Result<Vec<EntityId>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    entity_controller::move_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
        field,
        ids_to_move,
        new_index,
    )
    .context("moving entity relationship")
}
