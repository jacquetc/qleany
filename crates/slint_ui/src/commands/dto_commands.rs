// Generated by Qleany v1.7.0 from frontend_entity_commands.tera

//! Dto entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::direct_access::dto::DtoRelationshipField;
use common::types::EntityId;
use direct_access::DtoRelationshipDto;
use direct_access::{CreateDtoDto, DtoDto, UpdateDtoDto, dto_controller};

/// Create a new dto entity (orphan, no parent)
pub fn create_orphan_dto(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateDtoDto,
) -> Result<DtoDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dto_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating dto")
}
/// Create a new dto entity as child of owner
pub fn create_dto(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateDtoDto,
    owner_id: EntityId,
    index: i32,
) -> Result<DtoDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dto_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating dto")
}
/// Create multiple dto entities (orphan, no parent)
pub fn create_orphan_dto_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateDtoDto],
) -> Result<Vec<DtoDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dto_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating dto entities")
}
/// Create multiple dto entities as children of owner
pub fn create_dto_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateDtoDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<DtoDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dto_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating dto entities")
}
/// Get a dto entity by ID
pub fn get_dto(ctx: &AppContext, id: &EntityId) -> Result<Option<DtoDto>> {
    dto_controller::get(&ctx.db_context, id).context("getting dto")
}

/// Get multiple dto entities by IDs
pub fn get_dto_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<DtoDto>>> {
    dto_controller::get_multi(&ctx.db_context, ids).context("getting dto entities")
}

/// Get all dto entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_dto(ctx: &AppContext) -> Result<Vec<DtoDto>> {
    dto_controller::get_all(&ctx.db_context).context("getting all dto entities")
}

/// Update a dto entity
pub fn update_dto(ctx: &AppContext, stack_id: Option<u64>, dto: &UpdateDtoDto) -> Result<DtoDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dto_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating dto")
}

/// Update multiple dto entities
pub fn update_dto_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[UpdateDtoDto],
) -> Result<Vec<DtoDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dto_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating dto entities")
}

/// Update a dto entity with relationships
pub fn update_dto_with_relationships(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &DtoDto,
) -> Result<DtoDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dto_controller::update_with_relationships(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating dto with relationships")
}

/// Update multiple dto entities with relationships
pub fn update_dto_with_relationships_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[DtoDto],
) -> Result<Vec<DtoDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dto_controller::update_with_relationships_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating dto entities with relationships")
}

/// Remove a dto entity by ID
pub fn remove_dto(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dto_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing dto")
}

/// Remove multiple dto entities by IDs
pub fn remove_dto_multi(ctx: &AppContext, stack_id: Option<u64>, ids: &[EntityId]) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dto_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing dto entities")
}

/// Get a dto relationship
pub fn get_dto_relationship(
    ctx: &AppContext,
    id: &EntityId,
    field: &DtoRelationshipField,
) -> Result<Vec<EntityId>> {
    dto_controller::get_relationship(&ctx.db_context, id, field).context("getting dto relationship")
}

/// Get relationship IDs for multiple dto entities at once
pub fn get_dto_relationship_many(
    ctx: &AppContext,
    ids: &[EntityId],
    field: &DtoRelationshipField,
) -> Result<std::collections::HashMap<EntityId, Vec<EntityId>>> {
    dto_controller::get_relationship_many(&ctx.db_context, ids, field)
        .context("getting dto relationships (many)")
}

/// Get relationship count for a dto entity
pub fn get_dto_relationship_count(
    ctx: &AppContext,
    id: &EntityId,
    field: &DtoRelationshipField,
) -> Result<usize> {
    dto_controller::get_relationship_count(&ctx.db_context, id, field)
        .context("getting dto relationship count")
}

/// Get relationship IDs for a dto entity with pagination
pub fn get_dto_relationship_in_range(
    ctx: &AppContext,
    id: &EntityId,
    field: &DtoRelationshipField,
    offset: usize,
    limit: usize,
) -> Result<Vec<EntityId>> {
    dto_controller::get_relationship_in_range(&ctx.db_context, id, field, offset, limit)
        .context("getting dto relationship range")
}

/// Set a dto relationship
pub fn set_dto_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &DtoRelationshipDto,
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dto_controller::set_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("setting dto relationship")
}

/// Move (reorder) IDs within a dto relationship
pub fn move_dto_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    id: &EntityId,
    field: &DtoRelationshipField,
    ids_to_move: &[EntityId],
    new_index: i32,
) -> Result<Vec<EntityId>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    dto_controller::move_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
        field,
        ids_to_move,
        new_index,
    )
    .context("moving dto relationship")
}
